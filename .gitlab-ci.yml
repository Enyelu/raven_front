stages:          
  - deploy

deploy-job:      # This job runs in the deploy stage.
  stage: deploy
  script:
    - sudo npm install -g node@16.11.13
    - npm --version
    - npm run build
    - ls -ltr
    - cd build
    - pm2 serve build 3000 --spa 
  only: ['main']
